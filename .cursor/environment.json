{
  "name": "Project Confluence",
  "install": "python3 -m pip install torch --index-url https://download.pytorch.org/whl/cpu\npython3 -m pip install -r requirements.txt -r api_service/requirements.txt",
  "terminals": [
    {
      "name": "api",
      "command": "cd api_service && python3 -m uvicorn app:app --host 127.0.0.1 --port 8000",
      "description": "Project Confluence FastAPI service (AutoResearch jobs + Nigeria NSTG 2022 clinical-guidelines retrieval via TF-IDF fallback). Serves http://127.0.0.1:8000 (see /docs). For high-accuracy semantic search, run: pip install -r requirements-rag.txt"
    }
  ],
  "ports": [
    {
      "name": "api",
      "port": 8000
    }
  ]
}
